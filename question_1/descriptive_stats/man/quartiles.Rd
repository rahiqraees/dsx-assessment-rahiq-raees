% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_quartiles.R
\name{quartiles}
\alias{quartiles}
\alias{calc_q1}
\alias{calc_q3}
\alias{calc_iqr}
\title{Quartiles and interquartile range}
\usage{
calc_q1(x, na.rm = FALSE, type = 7)

calc_q3(x, na.rm = FALSE, type = 7)

calc_iqr(x, na.rm = FALSE, type = 7)
}
\arguments{
\item{x}{A numeric vector.}

\item{na.rm}{Logical. Should missing values be removed before computing?
Defaults to \code{FALSE}.}

\item{type}{Integer between 1 and 9 selecting the quantile algorithm; see
\code{\link[stats:quantile]{stats::quantile()}}. Defaults to 7.}
}
\value{
A numeric scalar.
}
\description{
\code{calc_q1()} returns the first quartile (25th percentile), \code{calc_q3()} the
third quartile (75th percentile) and \code{calc_iqr()} their difference
(\code{Q3 - Q1}). All three use \code{\link[stats:quantile]{stats::quantile()}} and default to its
\code{type = 7} definition (R's default).
}
\details{
Edge cases:
\itemize{
\item \code{x} empty (or all \code{NA} with \code{na.rm = TRUE}) returns \code{NA_real_} with a
warning.
\item \code{x} containing \code{NA} with \code{na.rm = FALSE} returns \code{NA_real_}.
\item A single value: \code{calc_q1()} and \code{calc_q3()} return it unchanged and
\code{calc_iqr()} returns 0.
\item Non-numeric input raises an error.
}

Note: the assessment brief's example lists Q1 = 2.5 and Q3 = 5.5 for
\code{c(1, 2, 2, 3, 4, 5, 5, 5, 6, 10)}. None of the standard \code{quantile()} types
give those values (type 7 gives 2.25 and 5), so this package follows the
standard definitions and exposes \code{type} so callers can pick one.
}
\examples{
data <- c(1, 2, 2, 3, 4, 5, 5, 5, 6, 10)
calc_q1(data)  # 2.25
calc_q3(data)  # 5
calc_iqr(data) # 2.75
}
